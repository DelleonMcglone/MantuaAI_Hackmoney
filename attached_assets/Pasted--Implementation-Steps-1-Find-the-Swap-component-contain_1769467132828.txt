**Implementation Steps:**

1. Find the Swap component/container (likely in `src/components/swap/` or rendered in `app.tsx`)

2. Wrap the chart and swap form in a flex container:
```tsx
<div style={{
  display: 'flex',
  flexDirection: 'row',
  gap: '20px',
  width: '100%',
  maxWidth: '1200px',
}}>
  {/* Left: Chart Panel */}
  <div style={{
    flex: '1.5',
    minWidth: '500px',
    background: isDark ? '#1a1a2e' : '#ffffff',
    borderRadius: '16px',
    border: `1px solid ${isDark ? '#2a2a45' : '#e2e8f0'}`,
    overflow: 'hidden',
  }}>
    <ChartPanel />
  </div>
  
  {/* Right: Swap Form */}
  <div style={{
    width: '400px',
    flexShrink: 0,
    background: isDark ? '#1e1e32' : '#ffffff',
    borderRadius: '16px',
    border: `1px solid ${isDark ? '#2a2a45' : '#e2e8f0'}`,
    padding: '24px',
  }}>
    <SwapForm />
  </div>
</div>
```

3. Remove any modal wrapper/overlay styling that was causing vertical stacking

4. Ensure the swap form no longer renders as a centered modal but as a fixed-width panel

**Responsive Behavior:**
- On screens < 900px wide: Stack vertically (flex-direction: column)
- Swap form should be full width when stacked
```tsx
// Add responsive styles
const containerStyle = {
  display: 'flex',
  flexDirection: window.innerWidth < 900 ? 'column' : 'row',
  gap: '20px',
};
```

**Critical Requirements:**
- Preserve ALL existing swap functionality (token selection, amount inputs, hook selection, fee display, swap execution)
- Preserve the settings gear icon in swap header
- Preserve the swap direction toggle button (↓ arrow between inputs)
- Preserve the "Change" button for hook selection
- Maintain dark/light mode theming
- Keep token icons and dropdowns working
- Do NOT break any wallet connection or transaction logic

**Files likely to modify:**
- `src/components/swap/SwapInterface.tsx` or similar
- `src/pages/app.tsx` (where swap is rendered)
- Any modal wrapper components

**Remove/Modify:**
- Remove modal centering styles (position: fixed, transform: translate, overlay background)
- Remove max-width constraints that limit the swap form to modal size
- Change from modal rendering to inline flex layout